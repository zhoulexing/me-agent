---
name: zlx-self-media
description: 自媒体选题与写作技能。通过联网搜索小红书、抖音、公众号、X、arXiv 等平台做选题，确认后再提供结构与逻辑势能选项，完成写作和标题生成。
---

# Self Media

## Overview
这个 skill 用于处理自媒体相关任务，重点不是泛泛聊内容，而是帮助用户持续找到值得做的题。

当前能力：
1. **选题研究**：基于联网搜索做多平台信号收集，围绕四类内容体系输出选题
2. **内容写作**：在用户确认选题后，先给结构和逻辑势能选项，再按要求写作
3. **标题优化**：基于选题或正文生成更能传播的标题
4. **小红书视频发布**：通过 browser-use 将本地视频发布到小红书创作者平台
5. **抖音视频发布**：通过 browser-use 将本地视频发布到抖音创作者平台

## References
| Reference | Trigger | Notes |
| --- | --- | --- |
| `references/topic-research.md` | 用户要找选题、做长期规划、做内容方向梳理、看最近有什么值得讲 | 默认优先使用；必须联网搜索 |
| `references/writing-principles.md` | 用户要写文章、写口播稿、整理表达方式、选择结构和逻辑势能 | 先确认选题，再给写作方案 |
| `references/title-principles.md` | 用户要起标题、优化标题、做标题候选 | 强调点击意愿，不要平铺直叙 |
| `references/xiaohongshu-video-post.md` | 用户要发布视频到小红书、发小红书 | 通过 browser-use 操作；发布前必须截图确认 |
| `references/douyin-video-post.md` | 用户要发布视频到抖音、发抖音 | 通过 browser-use 操作；发布前必须截图确认 |

## Workflows

### 1. 选题
- 先读取 `references/topic-research.md`
- 必须联网搜索，不允许只靠记忆直接给题
- 必须进行深度研究，不只看表面热点，还要判断背后的共性问题、讨论焦点和可讲深度
- 默认检索平台：小红书、抖音、公众号、X、arXiv
- 先看平台信号，再回到四类框架里组织选题

### 3. 写作
- 先读取 `references/writing-principles.md`
- 必须先确定选题，未确认前不要直接开写
- 选题确认后，先基于内容给出适合的结构选项和逻辑势能选项
- 等用户选定后，再开始正式写作
- 写完后再读取 `references/title-principles.md` 生成标题

### 4. 标题
- 读取 `references/title-principles.md`
- 基于已有主题或文稿生成标题，不要脱离内容胡乱起标题

### 5. 小红书视频发布
- 先读取 `references/xiaohongshu-video-post.md`
- 默认 headless 模式，使用 `--profile "用户1"` 复用登录状态
- 按流程操作：打开创作平台 → 上传视频 → 填写标题和正文 → 截图确认 → 发布
- 正文默认只包含两个固定标签：`#人工智能` `#视频日记`
- 发布前必须截图给用户确认，确认后才能点击发布
- 操作完成后关闭浏览器

### 6. 抖音视频发布
- 先读取 `references/douyin-video-post.md`
- 默认 headless 模式，使用 `--profile "用户1"` 复用登录状态
- 按流程操作：打开创作者平台 → 上传视频 → 填写标题和正文 → 截图确认 → 发布
- 正文默认只包含两个固定标签：`#人工智能` `#视频日记`
- 发布前必须截图给用户确认，确认后才能点击发布
- 操作完成后关闭浏览器

## Constraints
- 不要 AI 味太重
- 选题必须基于真实平台信号，不要凭空编趋势
- 选题研究不能停留在浅层热点罗列，必须做深度研究和交叉判断
- 涉及“最近、最新、现在流行什么”时，必须给出具体平台和时间判断
- 选题优先围绕四类内容体系，不要重新发散成很多平行方向
- 写作必须有对象感，默认直接对你说话
- 幽默感可选，但不能为了幽默破坏清晰度
- 结构和逻辑势能要分开判断
- 默认不要使用引号和破折号，除非用户明确要求
