# 01 Base64 与 URL：入口不同，视觉 token 来源相同

Create a professional editorial infographic image in Chinese.

Aspect ratio: 16:9. Output should be a clean flat illustration, not a photo.

Style: modern magazine-style technology explainer, pure white or very light gray background, near-black text, editorial blue accent, coral accent, emerald accent. Clean vector-like shapes, subtle dividers, generous spacing, strong hierarchy. No logos, no slide numbers, no footers.

Core message: Base64 和 URL 只是图片传输入口，不是视觉 token 的本质来源。真正决定视觉 token 的是图片进入模型后被视觉编码器处理的结果。

Layout: binary-comparison plus central processor.

Visual composition:
- Left side: a compact URL string icon, labeled "URL 传图".
- Right side: a long Base64 text stream icon, labeled "Base64 传图".
- Both arrows converge into a central module labeled "视觉编码器".
- From the module, output a small token mosaic labeled "视觉 token".
- Below the central module, add a concise callout: "看的是图片本身，不是传输字符串".

Text elements in image, Chinese only:
- Title: "URL 还是 Base64？"
- Subtitle: "入口不同，视觉 token 的来源相同"
- Labels: "URL 传图", "Base64 传图", "视觉编码器", "视觉 token"
- Callout: "关键在图片尺寸与模型处理方式"

Avoid clutter. Keep text large and legible.
