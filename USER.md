# USER.md - 这个用户怎么理解与协作

这份文件不是讲项目结构，而是讲这个用户的信息如何被理解、整理、存放、回查和同步。

## 用户资料的固定根目录

当你处理这个用户的资料时，默认根目录是：

```text
$HOME/.openclaw/workspace/me-agent
```

只要涉及查找文件、判断落盘位置、追加内容、同步笔记，都基于这个根目录理解，不要自行猜测别的路径。

## 用户信息的六个长期分区

这个用户的资料不是按文件类型组织，而是按以后怎么回查组织：

- `01-基本信息/`：稳定、基础、低频变化的信息
- `02-个人认知/`：用户对自己、判断方式、价值取向、行为偏好的认知
- `03-技术认知/`：用户对 AI、软件工程、产品、技术趋势的判断与思考
- `04-素材库/`：外部资料、链接摘录、灵感线索、可复用素材，与 Get 笔记里的 `素材库` 一一对应
- `05-信息记录/`：主题化沉淀、清单、模板、备忘和长期技术记录
- `06-流水账/`：当天发生的事、过程性记录，重点是按日期回查，并与 Get 笔记里的 `GetSeek录音卡` 一一对应

当用户给出一段内容时，先判断这段内容本质上属于什么：

- "我怎么看" -> `02-个人认知/`
- "技术上怎么看" -> `03-技术认知/`
- "这篇资料先存一下" -> `04-素材库/`
- "今天发生了什么" -> `06-流水账/`
- "这是一个长期技术研究点" -> `05-信息记录/技术.md`

如果用户已经明确指定目录，优先服从用户指定。

如果不确定该放 `04-素材库/`、`05-信息记录/` 还是 `06-流水账/`，按这个顺序判断：

- 外部资料、链接、摘录、灵感线索，就放 `04`
- 已经整理成主题化清单、模板、备忘、技术记录，就放 `05`
- 当天发生的事和过程记录，就放 `06`

## 用户相关的七类核心操作

| 操作类别 | 用户可能怎么说 | 你要做什么 | 默认落点 |
| --- | --- | --- | --- |
| 基本信息管理 | "查询基本信息""记录基本信息" | 查询 / 添加 / 更新 | `01-基本信息/` |
| 个人认知 | "查询个人认知""添加个人认知" | 查询 / 添加 / 更新 | `02-个人认知/` |
| 技术认知 | "查询技术认知""添加技术认知" | 查询 / 添加 / 更新 | `03-技术认知/` |
| 素材库 | "查看素材""保存素材""添加素材" | 查看 / 添加 / 更新 | `04-素材库/` |
| 信息记录 | "查看信息""记录信息" | 查看 / 添加 / 更新 | `05-信息记录/` |
| 流水账管理 | "查看流水账""添加流水账" | 查看 / 添加 / 更新 | `06-流水账/` |
| 笔记同步 | "同步到Get笔记""Get笔记下载" | 同步 / 下载 | Get 笔记知识库 |

## 处理用户资料时的通用顺序

每次接到任务，按这个顺序处理：

1. 先判断这是查、记、改、同步、下载中的哪一种
2. 再判断属于 `01 / 02 / 03 / 04 / 05 / 06` 哪个目录
3. 再判断是追加、更新，还是新建
4. 最后才执行落盘或同步

通用要求：

- 长期内容和当天记录不要混
- 已有目标文件存在时，优先追加或更新，不要随意重写
- 外部资料、网页摘录、别人观点，不要直接写成用户自己的认知
- 目标是放对、写清、可检索，不要为了完整而过度扩写
- 没判断清楚归属前，不要急着落盘
- 涉及对外动作、覆盖、批量同步或其他敏感操作时，先确认影响范围再执行

## 七类操作的具体规则

### 1. 基本信息管理

当用户要"查询基本信息""记录基本信息"时：

- 默认在 `01-基本信息/` 范围内查找、写入、更新
- 这类内容应当是稳定、基础、低频变化的信息
- 如果已有对应文件，优先更新或追加，不要无意义新建很多散文件
- 不要把临时状态、当天感受写进这里

### 2. 个人认知

当用户要"查询个人认知""添加个人认知"时：

- 默认在 `02-个人认知/` 范围内处理
- 这里只放用户对自己、判断方式、价值取向、行为偏好的认知
- 不要把外部文章摘要直接记成个人认知
- 不要替用户改写观点，只能整理、归纳、补充结构
- 文件命名格式：`YYYY-MM-DD-标题-[标签].md`（按时间倒序排列）

### 3. 技术认知

当用户要"查询技术认知""添加技术认知"时：

- 默认在 `03-技术认知/` 范围内处理
- 这里只放技术判断、工程理解、产品与 AI 相关思考
- 只有当内容已经形成用户自己的技术判断，才写入 `03-技术认知/`
- 文件命名格式：`YYYY-MM-DD-标题-[标签].md`（按时间倒序排列）

### 4. 信息记录

当用户要"查看信息""记录信息"时：

- 默认在 `05-信息记录/` 范围内处理
- 适合放主题化整理、模板、清单、备忘、长期技术记录
- 核心是以后按主题查，而不是按日期查
- 已有主题文件存在时，优先追加到原文件，不随意拆散
- 技术研究线索、工具研究项，优先放到 `05-信息记录/技术.md`

信息记录模板：

- 目标目录：`05-信息记录/`
- 文件命名：`[类目].md`，如 `金句.md`、`好物清单.md`、`模板方法.md`、`技术.md`
- 推荐类目：`金句.md`、`好物清单.md`、`模板方法.md`、`地点清单.md`、`重要备忘.md`、`技术.md`
- 同类信息放同一个文件，长期积累
- 不同条目用 `---` 分隔

```markdown
## [条目名称]

- **日期**：YYYY-MM-DD
- **类目**：[类目名]
- **标签**：xx,xx
- **来源**：xx

### 记录内容


### 备注
```

### 5. 素材库

当用户要"查看素材""保存素材""添加素材"时：

- 默认在 `04-素材库/` 范围内处理
- 这里专门存外部内容、灵感片段、链接摘录、可复用素材
- 本地结构要和 Get 笔记里的 `素材库` 一一对应，不要混进 `05-信息记录/`
- 已有主题文件存在时，优先追加到原文件，不随意分散

素材记录模板：

- 目标目录：`04-素材库/`
- 推荐文件：`技术素材.md`、`创作素材.md`、`产品素材.md`
- 重点是以后按主题回查和复用

```text
时间：YYYY-MM-DD hh:mm
标题：xxx
摘要：xxx
源链接：xxx

----
```

### 6. 流水账管理

当用户要"查看流水账""添加流水账"时：

- 默认在 `06-流水账/` 范围内处理
- 适合放当天发生的事、过程记录、临时观察
- 核心是以后按日期查
- 日志通常优先写入对应日期文件
- 本地日期文件要和 Get 笔记里的 `GetSeek录音卡` 一一对应
- 不要把素材汇总逻辑继续放在流水账里

流水账模板：

- 目标目录：`06-流水账/`
- 文件命名：`YYYY-MM-DD.md`
- 同章节多条记录用 `----` 分隔
- 不同类目分不同章节

```markdown
# YYYY-MM-DD

## 今日事件

## 今日书账

## 今日美食

## 今日新知

## 今日碎记

## 今日待办
```

单条记录格式：

```text
时间点：hh:mm（如 08:50）
标签：xx
原始内容：xx

----
```

### 7. 笔记同步

当用户要"同步到Get笔记""Get笔记下载"时：

- 先明确同步方向：是本地到 Get，还是 Get 到本地
- Get 知识库名称不带编号，本地文件夹名称带编号
- 同步和下载都必须按固定映射处理，不要自行猜知识库名或目录名
- 映射关系如下：
  - 知识库 `基本信息` ↔ 本地目录 `01-基本信息/`
  - 知识库 `个人认知` ↔ 本地目录 `02-个人认知/`
  - 知识库 `技术认知` ↔ 本地目录 `03-技术认知/`
  - 知识库 `素材库` ↔ 本地目录 `04-素材库/`
  - 知识库 `信息记录` ↔ 本地目录 `05-信息记录/`
  - `GetSeek录音卡` ↔ 本地目录 `06-流水账/`
- 如果是同步到 Get，就根据本地文件所在目录确定对应知识库
- 如果是从 Get 下载，就根据知识库名称确定对应本地目录
- 两边的核心类目名称必须一一对应，例如 `02-个人认知` 对应 `个人认知`
- `06-流水账/YYYY-MM-DD.md` 应对应 `GetSeek录音卡` 中同日期主题的内容，不要再按“素材”单独拆一套同步逻辑
- 如果涉及覆盖、批量同步、冲突处理，先说明影响范围
- 如果需要外部动作、网络传输或潜在覆盖，遵守敏感操作确认规则
